---
description: 改代码后必须在当前对话总结各脚本改动与整体思路；需 Editor 验收时附清单
alwaysApply: true
---

# 代码变更：改动总结 + Editor 验收清单（强制）

> **门禁**：凡创建或修改了代码文件，最终回复在「代码收尾」之前必须交代改了什么、为什么这样改。需要人在 Unity Editor 里确认时，必须附验收清单。未写总结即结束 = 违规。

## 触发条件

与 `lint-after-code-changes.mdc` 相同：新建或修改代码（尤其 `Assets/Scripts/**`）。

不触发：仅阅读、仅改 `.asset` / `.prefab` / `.meta`、仅改文档且无代码。

## 最终回复必须包含（填实，不得留空）

```markdown
## 改动总结
- **整体思路**：<为什么改、怎么串起来、新旧路径如何切换；删除了哪些旧入口>

### 各脚本
- `Path/File.cs`（新增/修改/删除）：<该文件做了什么、关键行为变化>
```

## 需要 Editor 验收时必须再附清单

出现任一情况即必须输出「请你在 Editor 确认」：

- IDE linter 无法代替 Unity 编译
- 改了运行时行为（进关、相机、刷怪、追打、状态机、输入、同步等）
- 新增/改了测试，需 Test Runner
- 需要用户在 Prefab / Inspector / 场景里绑引用或改配置

模板（按本次改动填实，不要照抄无关项）：

```markdown
## 请你在 Editor 确认
- 打开工程，等编译通过
- Play：<与本次改动相关的游玩路径；未改动的核心路径写「应与改前一致」>
- Test Runner：<相关测试类名；无则写「无新增/相关测试」>
- Inspector / Prefab（如有）：<要拖的引用、要勾的字段、要建的资产>
```

示例：

```markdown
## 请你在 Editor 确认
- 打开工程，等编译通过
- Play：单人进关、相机跟随、刷怪、敌人追打应与改前一致
- Test Runner：EnemyPerceptionTests、EnemyCombatRequestTests
```

## 禁止

- 只写「已改完」或只附「代码收尾」，不写各脚本与整体思路
- 用文件路径列表代替「每个脚本改了什么」
- 无关改动也堆一长串通用 Play 步骤；清单必须对应当次 diff
- 把本应 Editor 确认的编译/游玩/测试说成 Agent 已验证通过
