---
description: 重写逻辑或重构框架时不保留旧兼容层，避免无用代码残留
alwaysApply: true
---

# 重写/重构：禁止保留旧逻辑兼容层

## 目标

在进行代码逻辑重写、模块替换、框架重构时，避免为了“兼容旧实现”而残留死代码、旁路分支或双轨执行路径。

## 必须

1. 以**新逻辑为唯一真源**，删除被替代的旧入口、旧分支、旧适配层
2. 禁止同时保留 `Legacy/Old/V1/Fallback` 与 `New/V2` 双套运行逻辑
3. 若确实需要迁移窗口，必须由用户明确要求；否则默认直接切换并清理旧逻辑
4. 改动完成后，最终说明中明确列出本次删除了哪些旧路径或兼容代码

## 禁止

- 为了“保险”保留永不触发的 fallback 分支
- 新增仅用于过渡的 wrapper / adapter 但不移除旧调用链
- 保留未再被调用的旧方法、旧字段、旧事件注册
- 继续维护旧参数语义并在新逻辑中做隐式兼容映射（未经用户明确要求）

## 允许

- 一次性重命名、替换调用点并删除旧符号
- 在 PR/回复中说明破坏性变更影响和需要用户同步调整的地方
- 当用户明确要求“阶段迁移”时，按用户指定窗口保留临时兼容代码（并标注移除时机）
