---
description: 改代码后必须补注释、调用 ReadLints、在回复中提交「代码收尾」清单；未完成不得声称任务结束
alwaysApply: true
---

# 代码变更：注释 + Linter 自检（强制）

> **门禁**：凡创建或修改了代码文件，在向用户表示「已完成 / 好了 / 改完了」之前，必须完成本节全部步骤，并在最终回复附上「代码收尾」清单。未完成即结束 = 违规。

## 触发条件

以下任一情况即触发本规则：

- 新建或修改 `Assets/Scripts/**` 下的 `.cs`、`.asmdef`、Editor 脚本
- 新建或修改项目中其他代码文件（如 `docs/` 内可执行示例除外）

不触发：仅阅读代码、仅改 `.asset` / `.prefab` / `.meta`（见 `no-art-asset-edits.mdc`）

## 步骤 1 — 补充注释（必须先于 linter）

对**本次新增或改动的每一文件**补充注释。**最低要求（不可省略）**：

| 位置 | 必须 |
|------|------|
| 新增/改动的 `class`、`interface`、`struct`、`enum` | 类型顶：`///` 或 `//` 一行说明职责 |
| 新增/改动的 public / protected 方法、属性、事件 | 每个一行说明用途；有前置条件或副作用须写明 |
| 非显而易见的分支、算法、时序、帧窗口、状态转换、缓冲/连招逻辑 | 行内或块注释解释「为什么」 |
| 新增/改动的复杂 private 方法（>5 行或含非平凡分支） | 方法顶一行说明做什么 |

**禁止**：废话注释（`// return true`）、AI 元注释、只改代码不补注释。

**允许省略**： trivial 单行 getter/setter、显而易见的 `Awake` 组件缓存（除非含特殊回退逻辑）。

标识符英文；注释可用简短中文或英文。

## 步骤 2 — 调用 ReadLints（工具调用，不可口头代替）

1. **必须**调用 `ReadLints`，`paths` = 本次改动的**全部**文件路径（不得只查一个文件）
2. 若返回报错/警告且与本次改动相关 → 修复 → **再次** `ReadLints`，直至相关项清零
3. 若 `ReadLints` 无结果或明显未覆盖 Unity 编译（常见于无 `.sln` 的 Unity 项目）→ 在「代码收尾」清单中**明确写出**「IDE linter 无反馈，Unity 编译需在 Editor 确认」，不得假装已编译通过

## 步骤 3 — 最终回复必须附带「代码收尾」清单

有代码改动时，最终回复**末尾**必须包含以下模板（填实内容，不得留空占位）：

```markdown
## 代码收尾
- **注释**：已补充 `<文件1>`、`<文件2>` …
- **ReadLints**：已调用；结果 = 无相关报错 / 已修复 … / IDE 无反馈（Unity Editor 待确认）
- **未解决问题**：无 / `<具体项>`
```

无代码改动时不需此清单。

## 执行顺序

```
改代码 → 补注释(全部改动文件) → ReadLints(全部改动路径) → 修错 → 再 ReadLints → 附「代码收尾」清单 → 结束
```

## 禁止

- 跳过 `ReadLints` 工具调用，仅在文字中说「已检查 linter」
- 只注释或只 linter 其中一项就结束
- 遗漏同次改动中的关联文件
- 借修 linter/注释之名做大范围无关重构
