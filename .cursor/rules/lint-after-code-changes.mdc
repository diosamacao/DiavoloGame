---
description: 生成或修改代码后须补充注释、在当前对话内用 linter 检查并修复引入的错误
alwaysApply: true
---

# 代码变更：注释 + Linter 自检

## 必须

在完成任意代码文件的**创建或修改**后，于**当前对话内**完成以下两项，再向用户汇报完成：

### 1. 补充代码注释

对**本次新增或改动的代码**补充注释，使意图与关键逻辑可读：

| 位置 | 要求 |
|------|------|
| 新增/改动的 `class`、`interface`、`struct` | 类顶简短说明职责（`///` 或 `//`） |
| 新增/改动的 public / protected 成员 | 说明用途、前置条件或副作用 |
| 非显而易见的分支、算法、时序、帧窗口、状态转换 | 行内或块注释解释「为什么」 |
| 复杂私有方法 | 方法顶一行说明做什么 |

注释规范（与 `CONVENTIONS.md` 一致）：

- 标识符英文；注释可用简短中文或英文
- 只注释非 obvious 逻辑，不写废话式注释（如 `// increment i`）
- 不写「此文件由 AI 生成」等元注释
- 不为了凑注释而重复代码字面含义

### 2. Linter 检查

1. 对本次改动涉及的所有文件调用 linter（优先 `ReadLints`，范围限定为已改路径）
2. 若存在报错或警告且与本次改动相关，**在同一轮对话中修复**，直至相关 linter 问题清零
3. 若仍有无法在当前环境消除的问题，需明确说明原因与剩余项

## 范围

- 适用：`Assets/Scripts/**` 及本次任务中新建/修改的其他代码文件（`.cs`、`.asmdef`、Editor 脚本等）
- 不适用：`.asset`、`.prefab`、`.meta` 等由用户在 Unity Editor 维护的资产（见 `no-art-asset-edits.mdc`）

## 流程

```
改代码 → 补充注释 → ReadLints(改动文件) → 有错误则继续修 → 再次 ReadLints → 无相关错误后结束
```

## 注意

- 不要跳过注释或 linter 检查直接声称「已完成」
- 不要只检查单个文件而遗漏同次改动中的关联文件
- 修复与注释时保持最小 diff，不借机重构无关代码
