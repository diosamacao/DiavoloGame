---
description: 生成或修改代码后必须在当前对话内用 linter 检查并修复引入的错误
alwaysApply: true
---

# 代码变更：Linter 自检

## 必须

在完成任意代码文件的**创建或修改**后，于**当前对话内**立即执行 linter 检查：

1. 对本次改动涉及的所有文件调用 linter（优先 `ReadLints`，范围限定为已改路径）
2. 若存在报错或警告且与本次改动相关，**在同一轮对话中修复**，直至相关 linter 问题清零
3. 向用户汇报完成状态前，确认已做过上述检查；若仍有无法在当前环境消除的问题，需明确说明原因与剩余项

## 范围

- 适用：`Assets/Scripts/**` 及本次任务中新建/修改的其他代码文件（`.cs`、`.asmdef`、Editor 脚本等）
- 不适用：`.asset`、`.prefab`、`.meta` 等由用户在 Unity Editor 维护的资产（见 `no-art-asset-edits.mdc`）

## 流程

```
改代码 → ReadLints(改动文件) → 有错误则继续修 → 再次 ReadLints → 无相关错误后结束
```

## 注意

- 不要跳过检查直接声称「已完成」
- 不要只检查单个文件而遗漏同次改动中的关联文件
- 修复时保持最小 diff，不借机重构无关代码
